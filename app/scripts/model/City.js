define(["lib.use!lib.debug"], function(debug) {
  "use strict";

  var self = this;

  self.name = "";

  self.weather = {
    temperature: {
      average: 0,
      maximum: 0,
      minimum: 0
    },

    description: ""
  };


});